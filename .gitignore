__pycache__/
*.pyc
db.sqlite3
data/
staticfiles/
.env
venv/
*.egg-info/
.DS_Store
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# The real trained model file — large binary, shouldn't live in git history.
# Workstream A should hand this off separately (or via git-lfs / cloud
# storage) and each teammate/instance places it at ml/tensorflow_file.keras.
ml/tensorflow_file.keras
