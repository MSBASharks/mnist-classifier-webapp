# Web framework
Django==5.2.6
gunicorn==23.0.0

# Image / array handling used by the classify view
# numpy is pinned below 2.0 specifically because tensorflow-cpu 2.17.0
# requires numpy<2.0.0 — using numpy 2.1.x here caused a dependency
# resolution conflict during `pip install`. 1.26.4 is the last stable
# 1.x release and works fine with Pillow/Django too.
numpy==1.26.4
Pillow==11.0.0

# The real trained model is now wired in (see ml/model.py) — tensorflow-cpu
# is needed for tf.keras.models.load_model() and predict(). Using the -cpu
# build (not full tensorflow) keeps the Docker image smaller and skips
# unused GPU dependencies, since the Lightsail instance has no GPU anyway.
tensorflow-cpu==2.17.0

#a small library that lets Django serve its own collected static files.
#hopefully fixes some styling issues.
whitenoise==6.9.0
